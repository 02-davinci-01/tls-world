import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // the dev badge sits in the bottom-left corner, where the player launcher lives
  devIndicators: false,
};

export default nextConfig;
